# RTK bundle (Locus-managed)

Pinned version: see `version.txt`.

Prepare locally:

```bash
bun run rtk:bundle
```

Override download URL with `RTK_DOWNLOAD_BASE`. Disable runtime rewriting with `LOCUS_RTK_DISABLED=1`.
