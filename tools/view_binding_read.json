{
  "description": "Read a Unity SerializedProperty for a View Binding. Provide either bindingId from bindings.json or a concrete target. Package files are still read and edited through normal file tools.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "viewId": {
        "type": "string",
        "description": "View id containing bindings.json, for example material-inspector."
      },
      "bindingId": {
        "type": ["string", "null"],
        "description": "Optional binding id from bindings.json."
      },
      "target": {
        "type": ["object", "null"],
        "description": "Concrete binding target. Fields: kind, path, scenePath, objectPath, componentType, propertyPath."
      }
    },
    "required": ["viewId"]
  }
}
