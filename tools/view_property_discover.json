{
  "description": "Discover Unity SerializedProperty propertyPath values for a concrete View property target without writing data. Use this to validate unknown property paths before hardcoding them.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "target": {
        "type": "object",
        "description": "Concrete object target without propertyPath. Fields: kind, guid, path, scenePath, objectPath, componentType, componentIndex."
      },
      "query": {
        "type": ["string", "null"],
        "description": "Optional text search across property path, display name, field type, and assembly."
      },
      "fieldName": {
        "type": ["string", "null"],
        "description": "Optional field name or display name to find, for example speed or settings."
      },
      "fieldType": {
        "type": ["string", "null"],
        "description": "Optional C# field type name or full name. Base classes and interfaces match when Unity can reflect the type."
      },
      "maxDepth": {
        "type": ["number", "null"],
        "description": "Optional traversal depth cap. Defaults to 8."
      },
      "maxResults": {
        "type": ["number", "null"],
        "description": "Optional result cap. Defaults to 100, maximum 500."
      }
    },
    "required": ["target"]
  }
}
