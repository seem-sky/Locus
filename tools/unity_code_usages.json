{
  "description": "Find where a C# script class or one of its members is used inside Unity asset files (scenes, prefabs, ScriptableObject assets) — the serialized references that code_find_references cannot see. Use this before renaming or deleting a public method, UnityEvent handler or serialized field: zero code references does NOT mean unused, scene bindings break silently.\n\nModes:\n- Without 'member': lists every attach point of the script — which GameObjects in which scenes/prefabs carry the component, plus ScriptableObject instances of the class.\n- With 'member': searches the assets that reference the script for serialized uses of that member: UnityEvent bindings (m_MethodName, e.g. Button.onClick), serialized field values, and AnimationEvent function names in .anim clips.\n\nNotes:\n- Requires the pre-built asset reference database (rescan from the UI if results look stale).\n- UnityEvent matches are annotated with their resolved target; same-named methods on other scripts are flagged for verification.\n- Bindings that target a subclass inside an asset which never references this script directly are not found; check subclasses separately if relevant.",
  "parameters": {
    "type": "object",
    "properties": {
      "file_path": {
        "type": "string",
        "description": "Workspace-relative or absolute path to the .cs script file."
      },
      "member": {
        "type": "string",
        "description": "Optional method or serialized-field name declared in that script (e.g. 'TakeDamage', 'maxHealth'). Omit to list the script's attach points instead."
      },
      "max_results": {
        "type": "integer",
        "description": "Maximum usages to return. Default 100, max 400."
      }
    },
    "required": ["file_path"]
  }
}
