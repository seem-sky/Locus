{
  "description": "Load one or more specialized tools by name. The result returns the call path. In meta-tool mode, call targets through `tool_call`; in direct mode, loaded targets become native tools on the next model step.",
  "parameters": {
    "type": "object",
    "properties": {
      "tools": {
        "type": "array",
        "description": "Tool names to load. Multiple tools can be loaded in one call.",
        "items": {
          "type": "string"
        }
      }
    },
    "required": ["tools"],
    "additionalProperties": false
  }
}
