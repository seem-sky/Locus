{
  "description": "List valid View Packages under the selected Unity project's Locus/View directory. Results include displayPath for the user-visible tree path and packageRoot/packageRelPath for physical files. Use this only inside the /view workflow before creating or opening a View.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
