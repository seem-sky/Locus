{
  "description": "List valid View Packages under the selected Unity project's Locus/View directory and installed plugin Views. Results include displayPath for the user-visible tree path, packageRoot/packageRelPath for physical files, source, and plugin metadata when present. Use this inside /view and /plugin workflows before creating, editing, packaging, or opening a View.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
