{
  "description": "Reload a View Package after files have been edited. P0 validates the manifest and key files, emits a reload event to the View host when open, and returns the refreshed summary.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "viewId": {
        "type": "string",
        "description": "View id to reload, for example material-inspector."
      }
    },
    "required": ["viewId"]
  }
}
